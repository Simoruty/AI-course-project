token(IDToken).
tag(IDTag). %solo quelli banali


next(IDTokTag, IDTokTag).
depends(IDTag, IDTag).

appartiene(IDToken, Doc).


numero(IDToken). 
valuta(IDTag).
parola(IDToken). %qualsiasi token non numero, non simbolo, non valuta, non euro
tipo_richiesta(IDToken).

...



tax(IDToken, ['parola']).


neg(appartiene(IDToken, Doc)).
